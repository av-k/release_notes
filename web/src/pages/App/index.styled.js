import styled from 'styled-components';

const Wrapper = styled.div`
  width: 100%;
  max-width: 1120px;
  min-height: 520px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  box-shadow: -5px 5px 5px 0 rgba(0,0,0, 0.15), 5px 5px 5px 0 rgba(0,0,0, 0.15);
  padding: 30px 10px 50px;
  position: relative;
  background-color: #ffffff;
`;

export { Wrapper };
